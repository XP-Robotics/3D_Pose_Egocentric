# local machine config (copy of config.example.sh)
config.sh

# python
__pycache__/
*.pyc
.venv/
venv*/
*-venv/

# weights / models / large data (never commit — gated or huge)
_DATA/
pretrained_models/
*.ckpt
*.pt
*.pth
*.tar
*.tar.gz
MANO_*.pkl
mano_v1_2*

# per-clip artifacts / caches
artifacts/
cache/
*.pkl.gz
frames/
extract_frames/

# scratch
/tmp/
*.log
.DS_Store
